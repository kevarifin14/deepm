class Trainer():
    def __init__(self, market_history, agent):
        self.market_history = market_history
        self.agent = agent

    def train(self):
        for i in range(0, )

        batch = self.next_batch(index)

    def next_batch(self, index):
        market_history[index-]